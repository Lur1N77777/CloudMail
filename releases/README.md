# Releases

APK files are not committed to the Git repository.

For public downloads, upload release APKs to GitHub Releases. This local folder may contain the latest APK builds during development, but `*.apk` files are ignored by Git.
